version: 1
type: pipeline-template
name: Template with number Parameter
description: A template with a single number parameter
parameters:
  - name: someNumber
    displayName: A number
    type: number
multibranch:
  branchSource:
    github:
      repoOwner: paper-street-soap-co
      repository: ${repoName}
      scanCredentialsId: github-secret-text
